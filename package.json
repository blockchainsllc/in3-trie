{
  "name": "in3-trie",
  "version": "0.1.0",
  "description": "a implementation of the patricia merkle tree",
  "main": "js/src/index.js",
  "scripts": {
    "build": "rm -rf js && tsc -p .",
    "test": "npm run build && cp -rf ./test/testdata ./js/test/testdata && mocha ./js/test/*.js"
  },
  "author": "Simon Jentzsch",
  "license": "in3",
  "devDependencies": {
    "@types/mocha": "^5.2.6",
    "@types/node": "^10.14.1",
    "typescript": "^3.3.3333"
  },
  "dependencies": {
    "chai": "^4.2.0",
    "ethereumjs-util": "^6.0.0",
    "mocha": "^6.0.2"
  }
}
